<h1>Pets</h1>

{{#each content as |pet| }}
  <h2>{{pet.name}}</h2>
  <p>Owner: {{pet.owner}}</p>
  <p>Breed: {{pet.breed}}</p>
  <p>Age: {{pet.age}}</p>
{{/each}}

{{#link-to "index"}}Back{{/link-to}}

{{outlet}}
